# Only needed to RUN the test suite (tests/) -- the app itself has no build
# step and no dependencies of its own (see README). Installed by
# .github/workflows/test.yml in CI; for a human running these locally:
#   pip install -r tests/requirements.txt
#   playwright install --with-deps chromium
playwright==1.56.0
Pillow==12.2.0
